- dos32a\text\client
	* int21h.asm: Change CMP with a Jump table. Maybe also int10h.asm and int33h.asm.
	[v9.12.3] DONE. int21h.asm, int10h.asm, and int33h.asm all converted to
	256-entry jump tables indexed by AH (or AL for int33h). EBP used as the
	scratch register since no handler reads it live at entry. Sub-dispatchers
	for AH=44h (IOCTL) / AH=71h (Win95 LFN) in int21h, AH=4Fh (VESA) in int10h.


- pctest detects 8.00 as 7.1
	[v9.12.1+] STALE. Root cause was metadata mismatch (version.id said 8.00
	while in-binary dw 090Ch encoded 9.12). version.id updated to 9.12.x in
	v9.12.1; pctest's get_extender_type_ reads the in-binary version which
	has always been correct. The TODO comment predates the version-info
	cleanup.


- Write the press release of 8.00 dev, send to freedos, openwatcom, ...
	[N/A] Not a code item -- 2002-era publicity action by the (then-current)
	maintainer Javier Gutierrez. Preserved for historical context. The
	v8.0 press release was eventually published; see docs/historical/press.txt.

